name: CI Dashboard
on:
  push:
    branches: [develop, feat/dash]
  pull_request:
    branches: [main]
  schedule:
    - cron: "30 7 * * 0"
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

env:
  AZURE_PROJECT: dashboard
  PROJECT_REPOSITORY: Numbr-Tech/cubber-dash
  PROJECT_BRANCH: feat/deploy-container-app
  INFRA_ENV: ${{ github.ref_name == 'develop' && 'staging' || 'prod' }}

jobs:
  build-push-images:
    uses: ./.github/workflows/templates/build-push-template.yml
