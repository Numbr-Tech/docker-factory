name: CI Neo

on:
  push:
    branches: [develop]
  pull_request:
    branches: [main]
  schedule:
    - cron: "30 7 * * 0"
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

env:
  FQDN: azurecr.io
  DOCKERFILE_ROOT: apps
  PROJECT: neo

jobs:
  build-push-neo-images:
    strategy:
      matrix:
        include:
          - app: api
            arch: amd
            os: ubuntu-latest
          - app: api
            arch: arm
            os: ubuntu-22.04-arm
          - app: orchestrator
            arch: amd
            os: ubuntu-latest
          - app: orchestrator
            arch: arm
            os: ubuntu-22.04-arm
          - app: planner
            arch: amd
            os: ubuntu-latest
          - app: planner
            arch: arm
            os: ubuntu-22.04-arm
          - app: agents
            arch: amd
            os: ubuntu-latest
          - app: agents
            arch: arm
            os: ubuntu-22.04-arm
          - app: customer
            arch: amd
            os: ubuntu-latest
          - app: customer
            arch: arm
            os: ubuntu-22.04-arm

    runs-on: ${{ matrix.os }}

    steps:
      - name: Generate JWT
        id: generate_jwt
        run: |
          APP_ID=1560053
          PRIVATE_KEY="${{ secrets.GH_APP_SECRET }}"
          IAT=$(date +%s)
          EXP=$((IAT + 540))
          HEADER='{"alg":"RS256","typ":"JWT"}'
          PAYLOAD="{\"iat\":$IAT,\"exp\":$EXP,\"iss\":$APP_ID}"
          base64url() { openssl base64 -A | tr '+/' '-_' | tr -d '='; }
          ENCODED_HEADER=$(echo -n "$HEADER" | base64url)
          ENCODED_PAYLOAD=$(echo -n "$PAYLOAD" | base64url)
          SIGNATURE=$(echo -n "${ENCODED_HEADER}.${ENCODED_PAYLOAD}" | \
            openssl dgst -sha256 -sign <(echo "$PRIVATE_KEY") | \
            base64url)
          JWT="${ENCODED_HEADER}.${ENCODED_PAYLOAD}.${SIGNATURE}"
          echo "jwt=$JWT" >> $GITHUB_OUTPUT

      - name: Get App Token
        id: get_token
        run: |
          INSTALLATION_ID=75078289
          TOKEN=$(curl -s -X POST \
            -H "Authorization: Bearer ${{ steps.generate_jwt.outputs.jwt }}" \
            -H "Accept: application/vnd.github+json" \
            https://api.github.com/app/installations/$INSTALLATION_ID/access_tokens | jq -r .token)
          echo "token=$TOKEN" >> $GITHUB_OUTPUT

      - name: Set environment-specific variables
        id: env_config
        run: |
          if [[ "${GITHUB_REF}" == "refs/heads/develop" ]]; then
            echo "registry=pprodnbtregistry" >> $GITHUB_OUTPUT
            echo "subscription=${{ secrets.AZURE_SUBSCRIPTION_ID_PPROD }}" >> $GITHUB_OUTPUT
          else
            echo "registry=nbtreg" >> $GITHUB_OUTPUT
            echo "subscription=${{ secrets.AZURE_SUBSCRIPTION_ID }}" >> $GITHUB_OUTPUT
          fi

      - name: Checkout Neo repo
        uses: actions/checkout@v4
        with:
          repository: Numbr-Tech/Neo
          token: ${{ steps.get_token.outputs.token }}
          ref: dev

      - name: Bazel build image
        run: |
          bazel run //apps/${{ matrix.app }}:${{ matrix.app }}_load

      - name: Retag Bazel image for ACR push
        run: |
          SOURCE_IMAGE="${{ env.PROJECT }}-${{ matrix.app }}"
          IMAGE_TAG="${{ matrix.arch }}-${{ github.sha }}"
          if [[ "${GITHUB_REF}" == "refs/heads/develop" ]]; then
            TARGET_IMAGE="${{ steps.env_config.outputs.registry }}-aucgecdkece6b5d7.${{ env.FQDN }}/${{ env.PROJECT }}-${{ matrix.app }}:${IMAGE_TAG}"
          else
            TARGET_IMAGE="${{ steps.env_config.outputs.registry }}.${{ env.FQDN }}/${{ env.PROJECT }}-${{ matrix.app }}:${IMAGE_TAG}"
          fi
          echo "Retagging $SOURCE_IMAGE -> $TARGET_IMAGE"
          docker tag "$SOURCE_IMAGE" "$TARGET_IMAGE"
          echo "TARGET_IMAGE=$TARGET_IMAGE" >> $GITHUB_ENV

      - name: Log in to Azure with federated credentials
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ steps.env_config.outputs.subscription }}

      - name: Log in to Azure Container Registry
        run: az acr login --name ${{ steps.env_config.outputs.registry }}

      - name: Push Docker image
        run: docker push ${{ env.TARGET_IMAGE }}
